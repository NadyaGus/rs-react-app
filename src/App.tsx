import { Routes } from 'react-router';

export const LS_KEY = 'NADYA_GUS_KEY';

const App = () => {
  return (
    <div className={'app-container'}>
      <Routes></Routes>
    </div>
  );
};

export default App;
